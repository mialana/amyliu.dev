<menu class="gap-3xl desktop:gap-sm desktop:flex-col bg-primary-shade desktop:bg-primary-neutral flex p-2">
    <div class="desktop:block prose text-2xs hidden">
        <h1>Projects Selection</h1>
    </div>

    <select id="slim-select-projects-filter" multiple> </select>
</menu>

<script>
    import createProjectsSlimSelect from "../lib/projects/createProjectsSlimSelect";
    window.addEventListener("DOMContentLoaded", () => {
        createProjectsSlimSelect();
    });
</script>

{
    /*
@utility u-slim-select {
    --ss-primary-color: var(--color-primary-neutral);
    --ss-bg-color: var(--color-primary-neutral);
    --ss-font-color: var(--color-important-text);
    --ss-main-height: auto;
    --ss-border-radius: var(--radius-sm);

    & :where(.ss-main) {
        @apply shadow-sm;
    
        border-width: var(--border-md);
        border-color: --alpha(var(--color-primary-shadow) / 50%);

        & > .ss-values {
            padding: var(--gap-sm);

            & .ss-value {
                padding-inline: var(--gap-sm);

                & > * {
                    padding-inline: var(--gap-xs);
                }

                & > .ss-value-delete {
                    height: 0.8em;
                    width: 0.8em;
                    & > svg > path {
                        stroke: var(--color-important-text);
                    }
                }

                & > .ss-value-text {
                    line-height: 2em;
                    height: 2em;
                }
            }
        }

        & > svg.ss-arrow {
            display: none;
        }

        & > .ss-deselect {
            padding: var(--gap-md);
        }

        & * {
            color: var(--color-important-text);
        }
    }
}
*/
}
