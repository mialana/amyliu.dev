---
interface Props {
    title: string;
}

const { title } = Astro.props;
---

<div class="size-full p-4 text-center">
    <span class="prose">
        <h1 class="px-4 leading-relaxed">{title}</h1>
    </span>
    <div class="max-w-5xl mx-auto">
        <slot />
    </div>
</div>
